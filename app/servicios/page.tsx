import { StubPage } from '@/components/StubPage/StubPage';

export default function ServicesListPage() {
  return (
    <StubPage
      title="Servicios"
      milestone="M2 · Servicios público"
      description="Listado completo con filtros por categoría, búsqueda con debounce y paginación. Próximo milestone."
    />
  );
}
