import { StubPage } from '@/components/StubPage/StubPage';

export default function LoginPage() {
  return (
    <StubPage
      title="Ingresar"
      milestone="M2 · Auth (NextAuth v5)"
      description="Formulario de login con manejo de loading, error inline y validación Zod. Próximo milestone."
    />
  );
}
